@model CloudComputingProject.Models.OrderItem

@{
    ViewData["Title"] = "CreateOrderItem";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CreateOrderItem" method="post">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group" style="display:none">
                <label asp-for="ProductId" class="control-label">Product ID</label>
                <span class="form-control" readonly>@Model.ProductId</span>
                <input type="hidden" asp-for="ProductId" />
                <span asp-validation-for="ProductId" class="text-danger"></span>
            </div>
            <div class="form-group">
                  <img src="@ViewBag.Product.Url" alt="Product Image" class="regular-image" style="box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); border-radius: 0; width: 100px; height: 100px;" />
                </div>
            <div class="form-group">
                <label asp-for="Flavors" class="control-label">Select Flavors (up to @ViewBag.product.MaxFlavorsNumber) Flavors</label>
                <div id="flavor-selection">
                    <table>
                        <tr>
                            @foreach (var flavor in ViewBag.Flavors)
                            {
                                <td style="padding: 10px;">
                                    <div class="checkbox">
                                        <input type="checkbox" class="flavor-checkbox" name="flavors" data-max-flavors="@ViewBag.product.MaxFlavorsNumber" value="@flavor.Id" />
                                        <label for="SelectedFlavors">@flavor.FlavorName</label>
                                        <br />
                                        <img src="@flavor.FlavorUrl" alt="@flavor.FlavorName" class="round-image" height="70px" width="70px" />
                                    </div>
                                </td>
                            }
                        </tr>
                    </table>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Price" class="control-label">Price</label>
                <span style="font-size: 20px;">$@ViewBag.Product.Price.ToString("0.00")</span>
                <input type="hidden" asp-for="Price" />
                <span asp-validation-for="Price" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Add To Cart" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<style>
    .round-image {
        border-radius: 50%;
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Soft shadow */
        margin: 20px; /* Add margin around the images as needed */
    }
     .regular-image {
        box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Soft shadow */
        margin: 20px; /* Add margin around the images as needed */
    }
</style>



@section Scripts {
    <script>
        // JavaScript code to handle flavor selection
        $(document).ready(function () {
            $('.flavor-checkbox').on('change', function () {
        var selectedFlavors = $('.flavor-checkbox:checked');
        var maxFlavors = $(this).data('max-flavors');

        if (selectedFlavors.length > maxFlavors) {
            $(this).prop('checked', false);
                    alert('You can select up to ' + maxFlavors + ' flavors.');
        }
            });
        });
    </script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
